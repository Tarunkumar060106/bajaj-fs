const PORT = process.env.PORT || 3000;
const USER_ID = process.env.USER_ID || "yourname_ddmmyyyy";
const EMAIL_ID = process.env.EMAIL_ID || "you@example.com";
const COLLEGE_ROLL_NUMBER = process.env.COLLEGE_ROLL_NUMBER || "your_roll_number";

module.exports = {
  PORT,
  USER_ID,
  EMAIL_ID,
  COLLEGE_ROLL_NUMBER,
};
