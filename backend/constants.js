const PORT = process.env.PORT || 3000;
const USER_ID = process.env.USER_ID || "TarunkumarS_060106";
const EMAIL_ID = process.env.EMAIL_ID || "ts0147@srmist.edu.in";
const COLLEGE_ROLL_NUMBER = process.env.COLLEGE_ROLL_NUMBER || "RA2311003012062";

module.exports = {
  PORT,
  USER_ID,
  EMAIL_ID,
  COLLEGE_ROLL_NUMBER,
};
